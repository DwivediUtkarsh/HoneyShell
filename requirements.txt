# SSH server & protocol handling
paramiko==3.4.0

# MongoDB async driver (Motor) + sync driver (pymongo is a Motor dependency)
motor==3.3.2
pymongo==4.6.1

# Environment variable loading
python-dotenv==1.0.1
